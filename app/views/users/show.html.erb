<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <p class="user-introduction"><%= @user.introduction %></p>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
    <section>      
      <%= render 'shared/links' %>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.microposts.any? %>
      <h3><%= @title %></h3>
      <ol class="microposts">
        <% if @microposts_filtered.any? %>
          <p><%= @microposts_title_filtered %></p>
          <%= render @microposts_filtered %>
          <p><%= @microposts_title_other %></p>
          <%= render @microposts_other %>
        <% else %>
          <%= render @microposts_other %>
        <% end %>
      </ol>
      <%= will_paginate @microposts_other %>
    <% end %>
  </div>
</div>
